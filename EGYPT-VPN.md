# مصر VPN — دليل التشغيل

تطبيق ويب يفتح لك ما هو متاحٌ داخل مصر وأنت خارجها، بطريقتين مختلفتين جوهرياً:

| | بوّابة التصفّح | النفق الكامل |
|---|---|---|
| ما الذي يمرّ عبر مصر؟ | الصفحات المفتوحة داخل التطبيق فقط | ترافيك الجوال كلّه: تطبيقات ومتصفّح |
| يحتاج تثبيتاً؟ | لا | تطبيق WireGuard الرسميّ |
| يحتاج خادماً في مصر؟ | نعم (بروكسي) | نعم (خادم WireGuard) |
| يعمل مع البثّ المحميّ؟ | **لا** — الحماية الرقمية تمنعه | نعم غالباً |
| مناسب لـ | الأخبار، الخدمات الحكومية، بنك المعرفة | كلّ شيء |

> **صراحةً:** لا يستطيع أيّ تطبيق ويب أن يحوّل ترافيك جوالك كلّه — المتصفّح لا يملك هذه الصلاحية.
> من يَعِدك بذلك من صفحة ويب فهو يبيعك وهماً. لذلك جمع هذا التطبيق الأمرين: بوّابةً تعمل حالاً،
> ومولّدَ إعدادٍ لنفقٍ حقيقيّ.

الملفّات:

| الملفّ | العمل |
|---|---|
| `vpn.html` + `vpn-app.js` | الواجهة |
| `api/where.js` | يفحص: من أين تظهر أنت، ومن أين تظهر البوّابة |
| `api/proxy.js` | البوّابة: تجلب الصفحة من مصر وتعيد كتابة روابطها |
| `vpn-qr.js` | مولّد رمز QR (بلا مكتبات) |
| `vpn-wg.js` | مفاتيح X25519 داخل المتصفّح |
| `vpn-server-setup.sh` | تجهيز خادم WireGuard على جهاز في مصر |

---

## ١) بوّابة التصفّح

### متغيّرات البيئة

| المتغيّر | لازم؟ | المعنى |
|---|---|---|
| `EGYPT_PROXY` | نعم للبوّابة | بروكسي داخل مصر. `http://user:pass@host:8080` أو `socks5://user:pass@host:1080`. أكثر من واحد بفاصلة، فيُختار عشوائياً. |
| `VPN_CODE` | يُنصح به | رمزٌ يُطلب قبل التصفّح، حتّى لا تصير بوّابتك مفتوحةً للعالم. |
| `WG_ENDPOINT` | لا | يملأ خانة عنوان الخادم تلقائياً، مثل `41.33.10.20:51820`. |
| `WG_SERVER_PUBKEY` | لا | يملأ خانة المفتاح العامّ للخادم. |

بلا `EGYPT_PROXY` يخرج الطلب من مكان نشر التطبيق (أوروبا أو أمريكا غالباً)، والتطبيق يقول لك ذلك
صراحةً في صفحة «الحالة» بدل أن يوهمك بأنّك في مصر.

### إعداد بروكسي مصريّ

على نفس خادم مصر الذي ستستعمله للنفق:

```bash
sudo apt install -y squid apache2-utils
sudo htpasswd -c /etc/squid/passwd me          # اختر كلمة سرّ
sudo tee -a /etc/squid/squid.conf >/dev/null <<'EOF'
auth_param basic program /usr/lib/squid/basic_ncsa_auth /etc/squid/passwd
acl authenticated proxy_auth REQUIRED
http_access allow authenticated
http_access deny all
http_port 8080
EOF
sudo systemctl restart squid
```

ثمّ: `EGYPT_PROXY=http://me:كلمة-السرّ@عنوان-الخادم:8080`

**لا تترك البروكسي بلا كلمة سرّ.** المفتوح للعامّة يُستعمل في غير ما تحبّ، وتُنسب أفعاله إليك.

### ما تفعله البوّابة تحديداً

- تفتح نفق `CONNECT` (أو `SOCKS5`) إلى البروكسي المصريّ، ولا تخرج مباشرةً أبداً — فإن سقطت البوّابة
  ظهرت لك رسالة خطأ، ولم يُسرَّب عنوانك الحقيقيّ.
- تُعيد كتابة الروابط في HTML و CSS (`href`، `src`، `srcset`، `url()`، `@import`، النماذج، التحويلات).
- تحقن نصّاً يصحّح ما يفوت إعادةَ الكتابة: `fetch`، `XMLHttpRequest`، `history`، والروابط المضافة لاحقاً.
- تنزع `Content-Security-Policy` و`X-Frame-Options` و`integrity` حتّى تعمل الصفحة بعد التعديل.
- تعزل كعكات كلّ موقع بوسمٍ خاصّ، فلا يرى موقعٌ كعكات غيره.
- ترفض عناوين الشبكة الداخلية (`127.*`، `10.*`، `192.168.*`…).

### حدودها

`WebSocket` والبثّ المحميّ (DRM) وتسجيلُ الدخول ببصمة الجهاز — كلّها لا تمرّ. هذه ليست عيوباً
قابلة للإصلاح، بل حدودٌ لطبيعة بوّابات التصفّح. ما وراءها فهو عمل النفق.

---

## ٢) النفق الكامل

### أ) جهاز داخل مصر

خياران:

1. **خادم VPS في القاهرة** — أسهل وأثبت اتصالاً. تحقّق بعد الشراء أنّ عنوانه يظهر `EG` فعلاً؛
   السكربت يفحص ذلك ويحذّرك.
2. **حاسوب في بيت الأهل في مصر** (راسبيري باي مثلاً) — أبطأ، لكنّ عنوانه *منزليّ*، وهذا يهمّ:
   خدمات البثّ تحجب عناوين مراكز البيانات غالباً، ولا تحجب المنازل. يحتاج توجيه المنفذ
   `51820/UDP` من الراوتر، ويُفضَّل اسم DDNS لأنّ العنوان المنزليّ يتغيّر.

### ب) الخطوات

من تبويب «النفق» في التطبيق:

1. **ولّد مفاتيح الجوال** — تُولَّد داخل متصفّحك؛ المفتاح الخاصّ لا يُرسل إلى أيّ خادم، ويبقى في
   ذاكرة المتصفّح وحدها.
2. **نزّل سكربت الخادم** (يحمل مفتاحك العامّ)، ونفّذه على جهاز مصر:

   ```bash
   sudo bash vpn-server-setup.sh
   ```

   أو مباشرة:

   ```bash
   curl -fsSL https://<موقعك>/vpn-server-setup.sh -o wg.sh
   sudo CLIENT_PUBKEY='<مفتاحك-العامّ>' bash wg.sh
   ```

   يطبع في آخره سطرين: **المفتاح العامّ للخادم** و**العنوان**.
3. **انقل السطرين** إلى خانات التطبيق، ثمّ «أنشئ الإعداد والرمز».
4. **امسح الرمز** من تطبيق WireGuard الرسميّ: `+` ← «إنشاء من رمز QR».

### ج) خادم الأسماء

الافتراضيّ `163.121.128.134` و`163.121.128.135` (TE Data). استعمالُ أسماءٍ مصرية يجعل المواقع
تراك أقربَ إلى مصر، ويفتح ما يعتمد على DNS محلّي. إن تعطّل الاسمُ عندك فبدّله إلى `1.1.1.1`.

### د) أجهزة إضافية

أضف كتلة في `/etc/wireguard/wg0.conf` على الخادم:

```ini
[Peer]
PublicKey = <المفتاح العامّ للجهاز الجديد>
AllowedIPs = 10.7.0.3/32
```

ثمّ `sudo systemctl restart wg-quick@wg0`. لكلّ جهازٍ عنوانٌ مختلف ومفاتيحُ مختلفة.

### هـ) حين لا يعمل

| العرض | الغالب أنّه |
|---|---|
| النفق يتصل ولا إنترنت | التمرير أو الـ NAT. راجع `wg show` و`sysctl net.ipv4.ip_forward` |
| لا يتصل أصلاً | المنفذ مغلق. افتح `51820/UDP` في جدار المزوّد وفي الراوتر |
| يعمل لكنّ البثّ محجوب | الخدمة تحجب عناوين مراكز البيانات — جرّب العنوان المنزليّ |
| ينقطع كلّ دقيقة | زد `PersistentKeepalive` أو أنقص MTU إلى `1280` |

---

## ٣) الاختبارات

الأجزاء الحسّاسة مُختبَرة، ومنها ما لا يُقبل فيه الظنّ:

- **لا تسرّب**: إن سقطت البوّابة فالطلب يفشل ولا يخرج مباشرةً — مُختبَرٌ ببوّابة معطّلة عمداً.
- **إعادة الكتابة**: روابط نسبية ومطلقة و`srcset` و`url()` والتحويلات والضغط والكعكات المعزولة.
- **النفقان**: `CONNECT` و`SOCKS5`، ويُتحقَّق أنّ الطلب مرّ فعلاً بالنفق لا بجواره.
- **رمز QR**: يُرسَم ثمّ يُصوَّر من المتصفّح ثمّ يُفكّ بقارئ مستقلّ، ويُقابَل بالإعداد الأصليّ.
- **المفاتيح**: مقابَلةٌ بمكتبة `crypto` في Node، ومتّجهُ RFC 7748، في مساري WebCrypto والتنفيذ الداخليّ.

---

## ٤) الخصوصية والنظام

- تجاوزُ الحجب الجغرافيّ لمتابعة محتوى بلدك وأنت مسافر أمرٌ اعتياديّ، وقد يخالف شروط بعض الخدمات؛
  والمسؤولية عليك.
- ما تتصفّحه يمرّ بخادمك أنت، ويمكن أن يُسجَّل عليه.
- تطبيقات البنوك ترفض الشبكات الخاصّة كثيراً، وقد تُقفل الحساب مؤقّتاً. لا تستعملها عبر النفق.
- المفتاح الخاصّ في ذاكرة المتصفّح: امسحه من زرّ «امسح» على جهازٍ مشترك.
